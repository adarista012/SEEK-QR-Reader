import 'package:flutter/material.dart';

class QrReaderView extends StatelessWidget {
  const QrReaderView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('QR'), centerTitle: true));
  }
}
